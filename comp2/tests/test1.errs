!
!               Test Program with errors
!
PROGRAM test;
VAR x, y;

PROCEDURE muck ( REF x );
y;              ! VAR omitted
BEGIN
    y := x + 234 * y;
;               ! END omitted

BEGIN           ! main program starts here
    muck( x );
END             ! missing '.'
